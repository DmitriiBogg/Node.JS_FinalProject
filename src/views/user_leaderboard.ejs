<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Leaderboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>User Leaderboard</h1>
        <% leaderboard.forEach((user, index) => { %>
            <div class="list-container">
                <h2>#<%= index + 1 %> <%= user.email %></h2>
                <p>Total Character Level: <%= user.totalLevel %></p>
                <p>Completed Quests: <%= user.totalQuests %></p>
                <p>Achievements Earned: <%= user.totalAchievements %></p>
                <p>Overall Rating: <%= user.rating %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>
