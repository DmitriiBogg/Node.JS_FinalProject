<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Character Leaderboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Character Leaderboard</h1>
        <% characters.forEach((character, index) => { %>
            <div class="list-container">
                <h2>#<%= index + 1 %> <%= character.name %> (Level: <%= character.level %>)</h2>
                <p>Gold: <%= character.gold %></p>
                <p>Experience: <%= character.experience %> XP</p>
                <p>Achievements: <%= character.achievements.length %></p>
            </div>
        <% }); %>
    </div>
</body>
</html>
